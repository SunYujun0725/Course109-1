//
//  main.c
//  HW W5-2-2
//
//  Created by 孫渝鈞 on 2020/10/3.
//  Copyright © 2020 孫渝鈞. All rights reserved.
//

#include <stdio.h>

int main()
{
      int num;
      scanf("%d",&num);
      
        int divisor = 2, count;
       
        while(num != 1) {
              count = 0;
              while(num % divisor == 0) {
                num =num/divisor;
                count++;
              }
            if(count > 0 ) {
                if(count >=1)
                    printf("%d",divisor);
                    printf(" ");
                    printf("%d",count);
                    printf("\n");
            }
       divisor++;
    }
          return 0;
  
}
